name: ci

on:
  push:
    branches:
      - main
  schedule:
    - cron: "0 0 * * *"

jobs:
  autogreen:
    runs-on: ubuntu-latest
    
    permissions:
      contents: read/write
  
    steps:
      - name: Clone repository
        uses: whossssss

      - name: Auto green
        run: |
          git config --local user.email "hmnsmith02@gmail.com"
          git config --local user.name "whossssss"
          git remote set-url origin https://${{ github.actor }}:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}
          git pull --rebase
          git commit --allow-empty -m "a commit a day keeps your girlfriend away"
          git push
